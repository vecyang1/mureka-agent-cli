{
  "name": "mureka-agent-cli",
  "version": "0.1.0",
  "description": "Unofficial community CLI for the official Mureka API.",
  "type": "module",
  "bin": {
    "mureka": "./src/cli.js"
  },
  "scripts": {
    "mirror:docs": "node scripts/mirror-docs.mjs",
    "test": "node --test",
    "check": "npm test && node src/cli.js --help >/dev/null && node src/cli.js costs --format json >/dev/null"
  },
  "engines": {
    "node": ">=21"
  },
  "license": "AGPL-3.0-or-later"
}
